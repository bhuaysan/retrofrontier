{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capabilities for the RetroFrontier desktop window.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
